export default async function SemestersPage() {
    // const semesters = await db
    //     .select()
    //     .from(seasons)
    //     .where(eq(seasons.status, "Active"))
    //     .limit(1)
    //     .execute();

    return (
        <div>
            <h1>Semesters</h1>
            <pre>In Progress</pre>
        </div>
    );
}
