const config = {
    printWidth: 100,
    tabWidth: 4,
    semi: true,
    trailingComma: "es5",

    bracketSpacing: true,
    arrowParens: "always",

    plugins: ["prettier-plugin-tailwindcss", "prettier-plugin-packagejson"],
};

export default config;
