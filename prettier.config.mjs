const config = {
    printWidth: 100,
    tabWidth: 4,
    semi: true,
    trailingComma: "es5",

    bracketSpacing: true,
    arrowParens: true,

    plugins: ["prettier-plugin-tailwindcss"],
};

export default config;
